<?xml version="1.0" encoding="utf-8"?>
<resources>

    <string name="api_base_uri">https://api.spark.io</string>

    <!-- ATTENTION: This should be NONE for any production build; setting it to anything else
    will log HTTP headers, meaning that the user's token (and other potentially sensitive info)
    would be logged in plaintext! -->
    <string name="http_log_level">NONE</string>

</resources>